# react_barcode

Expo + React Native (TypeScript) starter for a barcode / camera experiment.

## Stack

Expo ~57, React Native, TypeScript.

## Run

```bash
npm install
npx expo start
```

Then open Android, iOS, or web from the Expo CLI. Notes for this project also live in `LEARNING.md`.
